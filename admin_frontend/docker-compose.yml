  admin_ui:
    build: ./admin-ui
    env_file:
      - ../.env
    environment:
      VITE_API_BASE: ${VITE_API_BASE:-http://localhost:8000}
      VITE_BASE_DOMAIN: ${BASE_DOMAIN}
    ports:
      - "5173:5173"
    depends_on:
      - api
