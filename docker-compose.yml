services:
  db:
    extends:
      file: ./backend/docker-compose.yml
      service: db
    env_file:
      - .env
    ports:
      - "5432:5432"

  api:
    extends:
      file: ./backend/docker-compose.yml
      service: api
    env_file:
      - .env
    environment:
      PYTHONPATH: /app
    ports:
      - "${API_PORT:-8000}:8000"
    depends_on:
      db:
        condition: service_healthy

  admin_ui:
    build: ./admin_frontend/admin-ui
    env_file:
      - .env
    environment:
      VITE_API_BASE: ${VITE_API_BASE:-http://api:8000}
      VITE_BASE_DOMAIN: ${BASE_DOMAIN}
    ports:
      - "5173:5173"
    depends_on:
      - api

  customer_ui:
    build: ./customer_frontend/customer-ui
    env_file:
      - .env
    environment:
      VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://api.test.myitnetwork.de}
      VITE_API_HOST: ${VITE_API_HOST:-}
      VITE_API_PORT: ${VITE_API_PORT:-443}
      VITE_API_PROTOCOL: ${VITE_API_PROTOCOL:-https}
      VITE_API_SUBDOMAIN: ${VITE_API_SUBDOMAIN:-api}
      VITE_BASE_DOMAIN: ${VITE_BASE_DOMAIN:-test.myitnetwork.de}
    ports:
      - "6173:6173"
    depends_on:
      - api

volumes:
  pgdata:
