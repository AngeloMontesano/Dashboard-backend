############################
# Global
############################
TZ=Europe/Berlin

# Zentrales NAS Root
DATA_ROOT=/volume1/docker/lagersoftware_saas


############################
# Backend / Compose / DB (aktiv)
############################
API_PORT=8000
ENVIRONMENT=prod
APP_VERSION=0.1.0
GIT_COMMIT=your-git-commit

ADMIN_API_KEY=change-me
JWT_SECRET=please-set-a-long-random-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRES_MIN=15
REFRESH_TOKEN_EXPIRES_DAYS=30
REFRESH_TOKEN_GRACE_MIN=5

POSTGRES_DB=lager
POSTGRES_USER=lager
POSTGRES_PASSWORD=change-me
DATABASE_URL=postgresql+asyncpg://lager:change-me@db:5432/lager

BASE_DOMAIN=example.com
BASE_ADMIN_DOMAIN=admin.example.com

POSTGRES_DB_TEST=lager_test
DATABASE_URL_TEST=postgresql+asyncpg://lager:change-me@db_test:5432/lager_test

SEED_TENANT_SLUG=kunde1
SEED_TENANT_NAME=Kunde 1
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=change-me
SEED_ADMIN_ROLE=owner


############################
# Backend Defaults (Code, aktiv)
############################
BUILD_TIMESTAMP=unknown
BUILD_BRANCH=unknown
IMAGE_TAG=unknown


############################
# Backend NAS Pfade
############################
POSTGRES_DATA_DIR=${DATA_ROOT}/postgres/data

API_LOGS_DIR=${DATA_ROOT}/backend/logs
API_STORAGE_DIR=${DATA_ROOT}/backend/storage

# Nur falls Backend Code gemountet wird (Dev)
BACKEND_APP_DIR=${DATA_ROOT}/backend/app


############################
# Admin Frontend (Vite)
############################
ADMIN_UI_NODE_CACHE=${DATA_ROOT}/admin-ui/node-cache
ADMIN_UI_LOGS=${DATA_ROOT}/admin-ui/logs

# Nur Dev
ADMIN_UI_APP_DIR=${DATA_ROOT}/admin-ui/app


############################
# Customer Frontend (Vite)
############################
CUSTOMER_UI_NODE_CACHE=${DATA_ROOT}/customer-ui/node-cache
CUSTOMER_UI_LOGS=${DATA_ROOT}/customer-ui/logs

# Nur Dev
CUSTOMER_UI_APP_DIR=${DATA_ROOT}/customer-ui/app


############################
# Frontend (Vite Build-Time, aktiv)
############################
VITE_BASE_DOMAIN=example.com
VITE_TENANT_SLUG=kunde1
VITE_GRAFANA_URL=http://localhost:3000
VITE_BUILD_INFO=1.0.0
